node_modules/
dist/
.DS_Store
*.tsbuildinfo
data/fetch/.venv/
data/fetch/__pycache__/

# Personal holding data — never committed to this public repo. Now
# persisted server-side instead (gate.php's /api/portfolio, writing
# public/portfolio-data.json on the live server only) — this entry is
# just a safety net in case that filename ever shows up in a local
# checkout.
public/portfolio.json
public/portfolio-data.json

# Generated by the fetch pipeline (real, via CI) or generate_mock.py
# (local dev). Never committed — the scheduled workflow fetches fresh
# data and deploys it directly, so shipping stale JSON in git would
# just be misleading dead weight.
public/data/*/*.json
